let val;
let test = [1, 2, 3, 4];

val = window;
val = window.document;
val = document.all.length;
val = document.all.length;

val = test.length;


val = document.scripts;
val = document.forms;
val = document.links;
val = document.images;
val = document.images[0].classList.add('off');
// val[2] = 'on'; it's not possible this way
document.URL.normalize();
val = document.URL;
val = document.contentType;
val = document.getSelection();
console.log(val);